import { AppEnvironment } from '@my-workspace/shared/environments';

export const environment: AppEnvironment = {
  production: true,
  apiBaseUrl: 'https://api.project-two.example.com',
  appName: 'Project Two — Admin Console',
  version: '1.0.0',
  authTokenKey: 'project_two_token'
};
