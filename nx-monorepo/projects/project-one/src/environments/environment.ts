import { AppEnvironment } from '@my-workspace/shared';

export const environment: AppEnvironment = {
  production: false,
  apiBaseUrl: 'http://localhost:3000/api',
  appName: 'Project One — Analytics Portal',
  version: '1.0.0',
  authTokenKey: 'project_one_token'
};
